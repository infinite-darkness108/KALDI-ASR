# preconditioning matrix computation
# Config file for initializing neural network prior to
input-node name=input dim=13

#Component nodes
output-node name=output input=Append(Offset(input, -2), Offset(input, -1), input, Offset(input, 1), Offset(input, 2)) objective=linear
